/**
 * Identité GÉNÉRIQUE de la plateforme SaaS (white-label ready).
 * Aucune marque d'entreprise n'est codée en dur : chaque tenant affiche
 * sa propre marque dans son workspace après connexion.
 */
export const PLATFORM_NAME = 'ServiceDesk';
export const PLATFORM_TAGLINE = 'Enterprise Service Portal';
export const PLATFORM_COPYRIGHT = `© ${new Date().getFullYear()} ${PLATFORM_NAME} — ${PLATFORM_TAGLINE}`;
