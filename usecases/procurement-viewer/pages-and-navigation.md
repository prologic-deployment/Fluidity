# Pages & navigation: procurement_viewer

Catalog card (`/services`, `/services/procurement`) — public, coming-soon badge. `/apps/procurement` placeholder exists but is unreachable without a subscription (impossible by default). No sidebar/module pages. Role visible in `/abonnements/licences` assignment UI + role catalog.
