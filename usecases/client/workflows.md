# Workflows: CLIENT

```
Onboarding
TA creates fiche → regenerer-acces (provisional pwd) → client logs in → mustChangePassword → /profile/security → definitive pwd → portal unlocked
```

```
Request lifecycle (client view)
create (ticket/demande/changement) → support works (broadcast emails exclude client) → waiting-client → client resumes → resolved → client closes or reopens
```
