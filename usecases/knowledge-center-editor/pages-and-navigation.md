# Pages & navigation: editor

Catalog card (`/services`, `/services/knowledge_center`) — public, coming-soon badge. `/apps/knowledge_center` placeholder exists but is unreachable without a subscription (impossible by default). No sidebar/module pages. Role visible in `/abonnements/licences` assignment UI + role catalog.
