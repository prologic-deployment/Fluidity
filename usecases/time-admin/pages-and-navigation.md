# Pages & navigation: time_admin

Catalog card (`/services`, `/services/time_tracking`) — public, coming-soon badge. `/apps/time_tracking` placeholder exists but is unreachable without a subscription (impossible by default). No sidebar/module pages. Role visible in `/abonnements/licences` assignment UI + role catalog.
