# Pages & navigation: security_admin

Catalog card (`/services`, `/services/security_center`) — public, coming-soon badge. `/apps/security_center` placeholder exists but is unreachable without a subscription (impossible by default). No sidebar/module pages. Role visible in `/abonnements/licences` assignment UI + role catalog.
