# Pages & navigation: viewer

Catalog card (`/services`, `/services/monitoring`) — public, coming-soon badge. `/apps/monitoring` placeholder exists but is unreachable without a subscription (impossible by default). No sidebar/module pages. Role visible in `/abonnements/licences` assignment UI + role catalog.
