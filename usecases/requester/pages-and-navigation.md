# Pages & navigation: requester

This role changes NO page, sidebar entry, or guard outcome. Navigation is driven by the internal role + entitlements (see `manager/`, `agent/`, `viewer/`, `client/`). The role is VISIBLE in `/abonnements/licences` (assignment UI) and the roles catalog/matrix — display only.
