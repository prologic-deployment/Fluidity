# Pages & navigation: member

Catalog card (`/services`, `/services/collaboration`) — public, coming-soon badge. `/apps/collaboration` placeholder exists but is unreachable without a subscription (impossible by default). No sidebar/module pages. Role visible in `/abonnements/licences` assignment UI + role catalog.
