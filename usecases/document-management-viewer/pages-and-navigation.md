# Pages & navigation: viewer

Catalog card (`/services`, `/services/document_management`) — public, coming-soon badge. `/apps/document_management` placeholder exists but is unreachable without a subscription (impossible by default). No sidebar/module pages. Role visible in `/abonnements/licences` assignment UI + role catalog.
