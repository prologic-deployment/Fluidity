# Pages & navigation: fleet_admin

Catalog card (`/services`, `/services/fleet_management`) — public, coming-soon badge. `/apps/fleet_management` placeholder exists but is unreachable without a subscription (impossible by default). No sidebar/module pages. Role visible in `/abonnements/licences` assignment UI + role catalog.
