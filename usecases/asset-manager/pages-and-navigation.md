# Pages & navigation: asset_manager

Catalog card (`/services`, `/services/asset_management`) — public, coming-soon badge. `/apps/asset_management` placeholder exists but is unreachable without a subscription (impossible by default). No sidebar/module pages. Role visible in `/abonnements/licences` assignment UI + role catalog.
