# Pages & navigation: backup_manager

Catalog card (`/services`, `/services/backup_management`) — public, coming-soon badge. `/apps/backup_management` placeholder exists but is unreachable without a subscription (impossible by default). No sidebar/module pages. Role visible in `/abonnements/licences` assignment UI + role catalog.
