# Pages & navigation: viewer

Catalog card (`/services`, `/services/ai_assistant`) — public, coming-soon badge. `/apps/ai_assistant` placeholder exists but is unreachable without a subscription (impossible by default). No sidebar/module pages. Role visible in `/abonnements/licences` assignment UI + role catalog.
