# Pages & navigation: bi_admin

Catalog card (`/services`, `/services/business_intelligence`) — public, coming-soon badge. `/apps/business_intelligence` placeholder exists but is unreachable without a subscription (impossible by default). No sidebar/module pages. Role visible in `/abonnements/licences` assignment UI + role catalog.
