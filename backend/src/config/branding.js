/**
 * Identité GÉNÉRIQUE de la plateforme SaaS (white-label ready).
 * Le nom « Fluidity » ne doit plus apparaître : la plateforme est neutre
 * et chaque Tenant y applique sa propre marque (logo, couleurs, nom).
 */
module.exports = {
  PLATFORM_NAME: 'ServiceDesk',
  PLATFORM_TAGLINE: 'Enterprise Service Portal',
  PLATFORM_LOG_TAG: '[ServiceDesk]',
};
